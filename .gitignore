# Secrets / environment
.env
*.env
!.env.example

# Runtime data (encrypted user content, salt, backups) — never publish
data/
uploads/

# Dependencies
node_modules/
server/node_modules/

# OS / editor
.DS_Store
*.log

# Claude Code local config
.claude/
