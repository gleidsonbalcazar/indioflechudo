{
  "name": "indioflechudo-client",
  "version": "1.0.0",
  "description": "Client-side executor/ping for the relay (runs on the corporate machine)",
  "private": true,
  "scripts": {
    "build:ping": "esbuild src/relay-ping.js --bundle --platform=node --target=node18 --outfile=../data/dist/relay-ping.js",
    "build:executor": "esbuild src/executor.js --bundle --platform=node --target=node18 --outfile=../data/dist/executor.js"
  },
  "dependencies": {
    "https-proxy-agent": "^7.0.5",
    "socket.io-client": "^4.7.4"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
